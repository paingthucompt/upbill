// Supabase types removed. The client-side generated DB types are not used
// anymore because the project no longer includes the Supabase client.
// If you need lightweight types for API responses, declare them under
// `src/types` or inline the interfaces in the components that call the API.

export type SupabaseTypesRemoved = null;
