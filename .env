# Supabase environment variables removed. The project now uses a custom
# Express + PostgreSQL backend. Configure the frontend to talk to the API
# using VITE_API_BASE_URL (e.g. VITE_API_BASE_URL=http://localhost:3000/api)
